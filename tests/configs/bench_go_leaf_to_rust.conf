# Go nats-server as leaf connecting to Rust open-wire hub for hub-mode benchmarks.
# TCP clients on 6225, connects upstream to Rust hub leafnode port 6422.
listen: 127.0.0.1:6225
leafnodes {
  remotes = [ { url: "leaf://127.0.0.1:6422", compression: off } ]
}

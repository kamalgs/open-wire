# Rust open-wire cluster node B (TCP only) for UDP comparison benchmarks.
listen: 127.0.0.1:8022
server_name: rust-tcp-cluster-b
cluster {
  name: bench-tcp-cluster
  listen: 127.0.0.1:8122
  routes = [
    "nats-route://127.0.0.1:8121"
  ]
}

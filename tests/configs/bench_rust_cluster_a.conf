# Rust open-wire cluster node A for cluster benchmarks.
listen: 127.0.0.1:8001
server_name: rust-cluster-a
cluster {
  name: bench-cluster
  listen: 127.0.0.1:8101
  routes = [
    "nats-route://127.0.0.1:8102"
    "nats-route://127.0.0.1:8103"
  ]
}

# Rust open-wire cluster node A (TCP only) for UDP comparison benchmarks.
listen: 127.0.0.1:8021
server_name: rust-tcp-cluster-a
cluster {
  name: bench-tcp-cluster
  listen: 127.0.0.1:8121
  routes = [
    "nats-route://127.0.0.1:8122"
  ]
}

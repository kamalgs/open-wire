# Go native leaf node for benchmarks.
# Connects to the hub leafnode port and accepts clients on 4225.
listen: 127.0.0.1:4225
leafnodes {
  remotes = [ { url: "leaf://127.0.0.1:7422", compression: off } ]
}
